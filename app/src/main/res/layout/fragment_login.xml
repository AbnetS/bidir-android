<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    style="@style/fragment_root"
    tools:context=".ui.login.LoginFragment">

    <ScrollView
        android:layout_width="300dp"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/loginSponsorContainer"
        android:layout_centerInParent="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <ImageView
                android:id="@+id/loginLogo"
                android:layout_width="300dp"
                android:layout_height="150dp"
                android:contentDescription="@string/contentDescription" />

            <EditText
                android:id="@+id/loginUsernameInput"
                style="@style/input"
                android:layout_marginBottom="20dp"
                android:hint="@string/login_username_input_hint" />

            <EditText
                android:id="@+id/loginPasswordInput"
                style="@style/input"
                android:layout_marginBottom="16dp"
                android:hint="@string/login_password_input_hint"
                android:inputType="textPassword" />

            <!--<TextView-->
                <!--android:id="@+id/loginForgotPasswordLabel"-->
                <!--style="@style/label.green"-->
                <!--android:layout_gravity="end"-->
                <!--android:layout_marginBottom="20dp"-->
                <!--android:paddingBottom="8dp"-->
                <!--android:paddingTop="16dp"-->
                <!--android:text="@string/login_forgot_password_label" />-->

            <Button
                android:layout_marginTop="20dp"
                android:id="@+id/loginButton"
                style="@style/button.dark.green"
                android:text="@string/login_button" />

        </LinearLayout>
    </ScrollView>

    <RelativeLayout
        android:id="@+id/loginSponsorContainer"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:layout_alignParentBottom="true"
        android:background="@color/gray_light">

        <View
            android:id="@+id/loginAutoFillView"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true" />


    </RelativeLayout>
</RelativeLayout>