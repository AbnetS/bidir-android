<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="32dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingBottom="12dp" >

        <TextView
            style="@style/label.large"
            android:text="@string/dialog_acat_title"
            android:paddingRight="12dp"
            android:paddingEnd="12dp" />

        <TextView
            android:id="@+id/dialog_crop_title"
            style="@style/label.large" />
    </LinearLayout>

    <TextView
        android:id="@+id/dialog_label"
        style="@style/label.small" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="32dp"
        >

        <Button
            android:id="@+id/acatDialogCancelButton"
            style="@style/button.dark.gray.dialog"
            android:layout_weight="1"
            android:text="@string/sample_button_cancel" />

        <Button
            android:id="@+id/acatDialogOkButton"
            style="@style/button.dark.green.dialog"
            android:layout_marginLeft="4dp"
            android:layout_marginStart="4dp"
            android:layout_weight="1"
            android:text="@string/acat_dialog_ok_button"
             />
    </LinearLayout>

</LinearLayout>