package com.gebeya.mobile.bidir.ui.home.main.clients;

/**
 * Presenter test for {@link ClientsPresenter} presenter
 */
//@RunWith(MockitoJUnitRunner.class)
public class ClientsPresenterTest {
/*
    private ClientsContract.Presenter presenter;

    @Mock
    private DataSourceContract<Client> repository;

    @Mock
    private ClientsContract.View view;

    @Before
    @SuppressWarnings("unchecked")
    public void setUp() throws Exception {
        repository = (DataSourceContract<Client>) mock(DataSourceContract.class);
        presenter = new ClientsPresenter();
        view = mock(ClientsContract.View.class);
        presenter.attachView(view);
    }

    @Test
    public void testAttachment() throws Exception {
        System.out.println("Should correctly attach presenter to view");
        assertNotNull(presenter.getView());
    }

    @Test
    public void testShowClients() throws Exception {
        System.out.println("Should load clients when the presenter starts");
        presenter.start();
        verify(view).showClients();
    }

    @Test
    public void testClientSelected() throws Exception {
        System.out.println("Should retrieve an item from the repository when clicked");
        presenter.onClientSelected(0);
        verify(repository).getItem(0, null);
    }

    @Test
    public void testAddClient() throws Exception {
        System.out.println("Should open add client screen when add client button is pressed");
        presenter.onMonitorButtonClicked();
        verify(view).openAddClientScreen();
    }*/
}