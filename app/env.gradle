project.ext.set("loadParams", {
    fileName ->
        return getProps(fileName)
})

private Properties getProps(path) {
    Properties props = new Properties()
    props.load(new FileInputStream(file(path)))
    return props
}